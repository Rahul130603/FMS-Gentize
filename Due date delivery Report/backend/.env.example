JWT_SECRET=change-this-to-a-long-random-string
PORT=5000
# Optional: override where the SQLite database file is stored (default: backend/data/fms_delivery.sqlite)
# DATABASE_PATH=./data/fms_delivery.sqlite
