# Base URL for the Technical Query API. Leave as /api when this client is
# built into (and served by) the existing FMS SPA and the API is proxied
# under the same origin. Point it at the standalone backend for local dev.
VITE_API_BASE_URL=/api
